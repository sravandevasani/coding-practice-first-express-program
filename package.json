{
  "name": "get-a-string",
  "version": "1.0.0",
  "description": "Given an `app.js` file, write an API with path `/` using express JS to send `Express JS` text as a response.",
  "main": "app.js",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "4.17.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": []
}
